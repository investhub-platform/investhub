PORT=5000
MONGO_URI=mongodb://localhost:27017/investhub
PAYHERE_MERCHANT_ID=
PAYHERE_SECRET=
PAYHERE_CURRENCY=LKR
PAYHERE_SANDBOX=true
BACKEND_URL=https://your-public-backend.example
FRONTEND_URL=https://your-frontend.example
# =========================================
# AUTHENTICATION (JWT)
# =========================================
JWT_ACCESS_SECRET=your_access_secret_here     # Secret key for signing access tokens
JWT_REFRESH_SECRET=your_refresh_secret_here   # Secret key for signing refresh tokens
ACCESS_TOKEN_EXPIRES=15m                      # Access token lifetime (e.g., 15m, 1h)
REFRESH_TOKEN_EXPIRES=7d                      # Refresh token lifetime (e.g., 7d, 30d)
COOKIE_SECURE=false                           # Set true in production (HTTPS only)

# =========================================
# EMAIL (SMTP) CONFIGURATION
# =========================================
SMTP_HOST=smtp.gmail.com                      # SMTP provider host
SMTP_PORT=587                                 # SMTP port (587 for TLS)
SMTP_USER=your_email@example.com              # SMTP login email
SMTP_PASS=your_app_password_here              # SMTP app password
SMTP_FROM=InvestHub <your_email@example.com>  # Sender display name

TEST_USER_ID=
GEMINI_API_KEY=your_gemini_api_key_here
OPENROUTER_API_KEY=your_new_key_here
